// * primitive types => string, number, boolean, undefined, null
// * reference types => 배열, 객체 

// * primitive types는 원시타입이라고 하는데, 
// * 메모리의 고정 크기로 원시 값을 저장하고 해당 저장된 값을 변수가 직접적으로 가리키는 형태를 일컬음

// * 참조 타입의 변수는 실제 데이터가 저장된 주소를 참조하기에 참조(reference) 타입이라고 불리는 것